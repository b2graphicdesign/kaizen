<!DOCTYPE html>
<html>
<head>
  <title>Kaizen</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
</head>
<body>

<header>
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">
        <img alt="Brand" src="https://pbs.twimg.com/profile_images/668850149188210688/5EWJ2qwv_400x400.png" class="brand-image">
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if admin_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Admins<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "Log Out", "/admins/sign_out", method: :delete %></li>
            </ul>
          </li>
        <% end %> 
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Providers<span class="caret"></span></a>
          <ul class="dropdown-menu">
          <% if provider_signed_in? %>
            <li><%= link_to "Edit Profile", "/provider/#{current_provider.id}/edit_provider" %></li>
            <li><%= link_to "Log Out", "/providers/sign_out", method: :delete %></li>
          <% else %>
            <li><a href="/providers/sign_in">Log In</a></li>
          <% end %>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Patients<span class="caret"></span></a>
          <ul class="dropdown-menu">
          <% if patient_signed_in? %>
            <li><%= link_to "Edit Profile", "/patient/#{current_patient.id/edit_patient}" %></li>
            <li><%= link_to "Log Out", "/patients/sign_out", method: :delete %></li>
          <% else %>
            <li><a href="/patients/sign_in">Log In</a></li>
          <% end %>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Transportation<span class="caret"></span></a>
          <ul class="dropdown-menu">
          <% if transportation_signed_in? %>
            <li><%= link_to "Edit Profile", "/transportation/#{current_transportation.id/edit_transportation}" %></li>
            <li><%= link_to "Log Out", "/transportations/sign_out", method: :delete %>
          <% else %>
            <li><a href="/transportations/sign_in">Log In</a></li>
          <% end %>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Drivers<span class="caret"></span></a>
          <ul class="dropdown-menu">
          <% if driver_signed_in? %>
            <li><%= link_to "Edit Profile", "/driver/#{current_driver.id/edit_driver}" %></li>
            <li><%= link_to "Log Out", "/drivers/sign_out", method: :delete %></li>
          <% else %>
            <li><a href="/drivers/sign_in">Log In</a></li>
          <% end %>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
</header>
  <div class="container-fluid">
  <% flash.each do |name, message| %>
    <div class="alert alert-<%= name %> alert-dismissible" role="alert">
      <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button><p><%= message %></p>
    </div>
  <% end %>
  </div>

<div class="container">
  <%= yield %>
</div>

</body>
</html>
