<legend><h3>List of Providers</h3></legend>
<div class = "jumbotron">
  <% @providers.each do |provider| %>
    <div class ="row">
      <ul>
        <li>Providers: <a href = "/provider/<%= provider.id %>" ><%= provider.first_name %> <%= provider.last_name %></a></li>
      </ul>
    </div>
  <% end %>

  <%= link_to "Home page", "/", class: "btn btn-default" %>


</div>
